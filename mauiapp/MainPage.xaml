<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="mauiapp.MainPage"
             xmlns:viewmodel="clr-namespace:mauiapp" 
             x:DataType="viewmodel:MainPageViewModel">

    <Grid RowDefinitions="Auto, Auto, *" ColumnDefinitions="Auto, *, Auto" Padding="10" RowSpacing="0" ColumnSpacing="10">

        <Picker x:Name="setPicker" 
                SelectedIndexChanged="OnPickerSelectedIndexChanged" 
                Grid.Row="0" Grid.Column="1" 
                ItemsSource="{Binding SetList}" 
                FontSize="36" 
                HorizontalOptions="CenterAndExpand" 
                VerticalOptions="Center"
                ItemDisplayBinding="{Binding Name}"
                SelectedItem="{Binding SelectedSet}"/>

        <!-- I have to use WebViews here because Images can't support .svg images at runtime -->
        <WebView Source="{Binding SelectedSet.IconUrl}" Grid.Row="0" Grid.Column="0" BackgroundColor="AntiqueWhite" HeightRequest="100" WidthRequest="100"/>
        <WebView Source="{Binding SelectedSet.IconUrl}" Grid.Row="0" Grid.Column="2" BackgroundColor="AntiqueWhite" HeightRequest="100" WidthRequest="100"/>

        <Entry Placeholder="" Grid.Row="1" Grid.ColumnSpan="3" TextChanged="OnEntryTextChanged"/>
        
        <CollectionView Grid.Row="2" Grid.ColumnSpan="3" ItemsSource="{Binding CardList}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="viewmodel:CardData">
                    <Frame Padding="20">
                        <Grid ColumnSpacing="10" ColumnDefinitions="Auto, *, Auto">
                            <Label Text="{Binding Card.CollectorNumber}" FontSize="24" Grid.Column="0"/>
                            <Label Text="{Binding Card.Name}" FontSize="24" Grid.Column="1"/>
                            <Label Text="{Binding TotalCount}" FontSize="24" Grid.Column="2"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>

</ContentPage>
